let people = ['Juan', 'Niyi', 'Milan', 'Tara'];
console.log(people);


if (people[3]) { // undefined representa false
    console.log('En la posición 3 no existen elementos');
}

for (let i = 0; i < people.length; i++) {
    console.log(people[i]);
    
}